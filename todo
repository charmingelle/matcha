- clean all components
- make sure that unnecessary information isn't passed to components
- style everything
- get rid of unused variables
- prettify everything
- test everything
- remove .gitignore, todo from prod

- check what happens during photo upload






- checkout to prod
- git merge master
- replace port 3000 with 5000
- uncomment "Built React version" lines in server.js
- change folder in profile.js
- change src with public to build in all components
- cd client && npm run build && cd ..
- npm run server
- http://localhost:5000
