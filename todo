- clean all components
- make sure that unnecessary information isn't passed to components
- style everything
- get rid of unused variables
- add pagination to Users and everywhere where needed
- prettify everything
- test everything
- write readme
- check if chat messages can be added to bottom
- remove matcha.end.pdf, music, notes, pgadmin4-3.5.dmg, .gitignore, todo
- set location to be equal to current user location for demo for the filter to find someboby







- checkout to prod
- git merge master
- replace port 3000 with 5000
- uncomment "Built React version" lines in server.js
- cd client && npm run build
- cd .. && node server/server.js
- http://localhost:5000
